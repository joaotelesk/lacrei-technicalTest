import styled from "styled-components";

export const Main = styled.main`
  height: 100vh;
  width: 100vw;
  padding: 6rem 4rem;
  padding-top: 8rem;

  /* tablet */
  @media (max-width: 768px) {
    padding: 2rem 1rem;
    padding-top: 8rem;
  }
`;
